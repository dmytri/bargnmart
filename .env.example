# Bunny Database connection (production)
# If not set, uses local SQLite file ./data/bargn.db
BUNNY_DATABASE_URL=libsql://your-database.turso.io
BUNNY_DATABASE_AUTH_TOKEN=your-database-token

# Admin authentication
ADMIN_TOKEN=your-admin-token

# Server configuration
PORT=3000
